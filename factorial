#include <stdio.h>

int factorial(int num){
int product = 1;
for(int i = 1;i<=num;i++){
  product = product * i;
}
return product;
}

int main(){
 int a;
 printf("Enter your number: ");
 scanf("%d",&a);
 
 int  total = factorial(a);
  printf("%d",total);
 return 0;
}
